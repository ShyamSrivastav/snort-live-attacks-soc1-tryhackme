# Scenario 1 — Brute Force (Snort Challenge: Live Attacks)

## Overview

J&Y Enterprise, a technology‑focused coffee retailer, stores a super‑secret recipe in a digital safe. Attackers targeted the environment with a brute‑force campaign aimed at the service that controls access to the safe. Using Snort, the objective is to detect the brute‑force activity, author an IPS rule that blocks the attack, and validate that the attack has been stopped. When the attack is blocked correctly, the flag appears on the desktop.

This document records the approach, the evidence collected, and the final results of the scenario.

---

## Objectives

1. Start Snort in sniffer (console) mode and observe traffic to identify the attack source, target service, and port.
2. Craft a Snort IPS rule to stop the brute‑force attack.
3. Test the rule in console mode, then run Snort in IPS mode to block the attack.
4. Confirm the attack is blocked and collect the flag that appears on the desktop.
5. Document evidence (screenshots) and lessons learned.

---

## Detection & Mitigation — Snort Rules

**Detection (alert) rule:**
```snort
drop tcp any 22 <> any any (msg: "ssh login detected"; sid:10001; rev:1;)
```
Rule structure:
-  drop — blocks matching traffic when Snort runs in inline/IPS mode.
-  tcp — matches TCP traffic observed in the capture.
-  any 22 (source IP/port) and any any (destination IP/port) ensure the rule blocks any host targeting port 22.
-  <> — bidirectional direction operator.
-  sid and rev — unique identifier and revision for the rule.
---
## Evidence — Brute Force

The following section documents the eight screenshots collected for Scenario 1 (Brute Force). All screenshots are stored in the repository at **screenshots/s1_xx.png/**. Each entry includes the command used, a concise explanation of what the screenshot shows, and why it is relevant evidence.

### 1. Starting Snort sniffer in full packet dump mode - ![Starting Snort sniffer in full packet dump mode](/screenshots/s1_01.png)

**Command:** `sudo snort -X -l .`

Snort started in full packet dump mode with logs writing to the current directory.Shows Snort initialized with -X (full packet dump) and -l . (log to current directory). This is the capture step that produces raw packet logs for later analysis.

### 2. Log output file generated by Snort - ![Log output file generated by Snort](/screenshots/s1_02.png)

**Command:** `sudo snort -X -l .`

Snort created a log file such as snort.log.<epoch> in the current directory. Confirms that Snort wrote a log file (for example, snort.log.1758997776) to the specified folder. This file contains the captured packets.

### 3. Reading the log file with full packet dump (first 10 packets) - ![Reading the log file with full packet dump (first 10 packets)](/screenshots/s1_03.png)

**Command:** `sudo snort -r snort.log.1758997776 -X -n 10`

Snort reading the saved log file and displaying the first 10 packets with full packet details. Demonstrates how the saved log file is inspected using -r to read the file and -X for full packet details. This step locates suspicious packets for further inspection.

 ### 4. Inspecting a suspicious packet (SSH evidence) - ![Inspecting a suspicious packet (SSH evidence)](/screenshots/s1_04.png)

Packet detail showing source/destination IPs, ports, protocol (TCP) and destination port 22 (SSH). While inspecting packet dumps, a packet targeting TCP port 22 (SSH) was identified. Packet details such as flags, TTL, and ports provide evidence that SSH is the targeted service.

### 5. Grep the log file for SSH entries - ![Grep the log file for SSH entries](/screenshots/s1_05.png)

**Command:** `sudo snort -r snort.log.1759003117 -X | grep 'ssh'`

Filtering the log file for **_ssh_** keyword to focus on SSH-related traffic. This confirms repeated SSH activity and helps quantify the suspicious attempts.

### 6. Evidence of frequent SSH login attempts - ![Evidence of frequent SSH login attempts](/screenshots/s1_06.png)

Console output revealing multiple repeated SSH connection attempts indicating a brute-force pattern. The grep output and packet details demonstrate frequent login attempts via SSH, consistent with a brute-force attack pattern.

### 7. Adding blocking rule to local.rules - ![Adding blocking rule to local.rules](/screenshots/s1_07.png)

**Command:** `sudo nano /etc/snort/rules/local.rules`

Text editor showing a Snort drop rule added to /etc/snort/rules/local.rules. Shows the rule being authored to block SSH traffic to port 22. This is the remediation step where the detection is converted into a prevention rule.

**Custom Rule** `drop tcp any 22 <> any any (msg: "ssh login detected"; sid:10001; rev:1;)`

Rule structure:
-  `drop` — blocks matching traffic when Snort runs in inline/IPS mode.
-  `tcp` — matches TCP traffic observed in the capture.
-  any 22 (source IP/port) and any any (destination IP/port) ensure the rule blocks any host targeting port 22.
-  `<>` — bidirectional direction operator.
-  sid and rev — unique identifier and revision for the rule.

### 8. Blocking the attack in IPS mode and obtaining the flag - ![Blocking the attack in IPS mode and obtaining the flag](/screenshots/s1_08.png)

**Command:** `sudo snort -c /etc/snort/snort.conf -q -Q --daq afpacket -i eth0:eth1 -A full`

This screenshot documents Snort running with the custom drop rule in inline/IPS mode. Parameters used:

- `-C /etc/snort/rules/local.rules` — load the specified rules file.
- `-q` — quiet startup output.
- `-Q` — enable inline/queue mode for IPS operation (requires compatible DAQ).
- `--daq afpacket` — use the AFPacket DAQ module for packet capture/inline capability.
- `-i eth0:eth1` — define the interface pair for inline operation (example).
- `-A full` — enable full alert/logging mode.

Snort running in IPS mode dropping packets and the desktop flag file showing THM{...}. Shows Snort running in inline/IPS mode with AFPacket DAQ and full logging. After allowing the rule to run, the lab's flag.txt appears on the desktop. cat flag.txt displays the flag **_THM{81b7fef657f8aaa6e4e200d616738254}_**, confirming successful mitigation.

_End of Evidence._

---

## Questions & Answers

**Q1 — Stop the attack and get the flag (which will appear on your Desktop)**  
**Answer:** `THM{81b7fef657f8aaa6e4e200d616738254}`

**Q2 — What is the name of the service under attack?**  
**Answer:** `SSH`

**Q3 — What is the used protocol/port in the attack?**  
**Answer:** `TCP/22`

---

## Conclusion & Remakrs 

A brute-force attack targeting the SSH service (TCP/22) was identified and blocked using Snort. By analyzing captured packets and log data, the attack pattern was confirmed. A custom IPS rule was created and deployed in inline mode to drop malicious traffic, successfully preventing further attempts and triggering the flag — confirming effective mitigation.

**Key Takeaways**

- Snort can serve as both a detection and prevention tool.
- Log analysis is essential for identifying attack patterns.
- Custom rules convert detection capabilities into active defense.
- SSH (port 22) is a common brute-force target; continuous monitoring is vital.
- Testing rules before deployment ensures accuracy and stability.
